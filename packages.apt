# Ubuntu 24.04 LTS packages
# Install with: grep -v '^#' packages.apt | grep -v '^$' | xargs sudo apt install -y

# Shell & Terminal
fish
tmux

# Development Tools
git
neovim
ripgrep
fzf
direnv
tree

# Build essentials (needed for some manual installs)
build-essential
curl
wget
unzip

# Go (needed for sesh)
golang-go

# Clipboard support (for tmux/neovim)
xclip

# Video/Audio processing
ffmpeg

# Python (for neovim plugins)
python3-pip
python3-venv
